#ifndef TRANSCODE_H
#define TRANSOCDE_H
#include <cstddef> // Include the header file that defines size_t
#include <cstdint> // Include the header file that defines uint8_t

void test_transcode_doId(const size_t client_id);

#endif // TRANSOCDE_H